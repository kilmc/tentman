# `@tentman/mdsvex`

Mdsvex integration for Tentman projects. It resolves Tentman content components and directive syntax into mdsvex-compatible Svelte output.

## Install

```sh
npm install @tentman/mdsvex @tentman/core
```

Use this package in a Tentman-backed Svelte or SvelteKit site that already uses `mdsvex`.
