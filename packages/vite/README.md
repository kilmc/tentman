# `@tentman/vite`

Small Vite helpers for Tentman authoring workflows.

## Install

```sh
npm install @tentman/vite
```

The current export provides the development-time full reload plugin for Tentman content-component source changes.
